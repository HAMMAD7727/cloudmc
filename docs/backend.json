{
  "entities": {
    "CoinPackage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CoinPackage",
      "type": "object",
      "description": "Represents a coin package available for purchase in the Cloudverse store.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the coin package."
        },
        "name": {
          "type": "string",
          "description": "Name of the coin package (e.g., Starter Pack, Value Pack)."
        },
        "coinAmount": {
          "type": "number",
          "description": "Amount of coins included in the package."
        },
        "price": {
          "type": "number",
          "description": "Price of the coin package."
        },
        "bonusDescription": {
          "type": "string",
          "description": "Description of any bonus included with the package (e.g., 20% bonus coins)."
        },
        "isPopular": {
          "type": "boolean",
          "description": "Indicates if the package is marked as the most popular."
        }
      },
      "required": [
        "id",
        "name",
        "coinAmount",
        "price"
      ]
    },
    "Rank": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Rank",
      "type": "object",
      "description": "Represents a rank available for purchase in the Cloudverse store.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the rank."
        },
        "name": {
          "type": "string",
          "description": "Name of the rank (e.g., Warrior, Champion)."
        },
        "price": {
          "type": "number",
          "description": "Price of the rank."
        },
        "coinBonus": {
          "type": "number",
          "description": "Coin bonus given upon purchasing the rank. Can be null."
        },
        "perks": {
          "type": "array",
          "description": "Array of perks included with the rank.",
          "items": {
            "type": "string"
          }
        },
        "adminKey": {
          "type": "string",
          "description": "A secret key to authorize administrative actions."
        },
        "textColor": {
          "type": "string",
          "description": "The hex color code for the rank name text."
        },
        "gradientFrom": {
          "type": "string",
          "description": "The starting hex color code for a gradient effect on the rank name."
        },
        "gradientTo": {
          "type": "string",
          "description": "The ending hex color code for a gradient effect on the rank name."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL for a custom image/icon for the rank."
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "perks",
        "adminKey"
      ]
    },
    "NewsItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsItem",
      "type": "object",
      "description": "Represents a news item displayed on the Cloudverse store.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the news item."
        },
        "title": {
          "type": "string",
          "description": "Title of the news item."
        },
        "content": {
          "type": "string",
          "description": "Content of the news item."
        },
        "date": {
          "type": "string",
          "description": "Date of the news item.",
          "format": "date-time"
        },
        "adminKey": {
          "type": "string",
          "description": "A secret key to authorize administrative actions."
        }
      },
      "required": [
        "id",
        "title",
        "content",
        "date",
        "adminKey"
      ]
    },
    "Feedback": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Feedback",
      "type": "object",
      "description": "Represents user feedback submitted through the Cloudverse store.",
      "properties": {
        "playerName": {
          "type": "string",
          "description": "The name of the player submitting the feedback."
        },
        "rating": {
          "type": "number",
          "description": "The rating given by the player (1-5 stars)."
        },
        "message": {
          "type": "string",
          "description": "The feedback message entered by the player."
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time the feedback was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "playerName",
        "rating",
        "message",
        "submissionDate"
      ]
    },
    "StaffMember": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StaffMember",
      "type": "object",
      "description": "Represents a staff member displayed on the Cloudverse store.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the staff member."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the staff member's profile picture."
        },
        "rank": {
          "type": "string",
          "description": "The staff member's rank (e.g., Admin, Moderator)."
        },
        "roleDescription": {
          "type": "string",
          "description": "A brief description of the staff member's role."
        },
        "adminKey": {
          "type": "string",
          "description": "A secret key to authorize administrative actions."
        },
        "email": {
          "type": "string",
          "description": "The staff member's email address to link to their chat user.",
          "format": "email"
        }
      },
      "required": [
        "name",
        "imageUrl",
        "rank",
        "roleDescription",
        "adminKey"
      ]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "A message in the live chat.",
      "properties": {
        "text": {
          "type": "string",
          "description": "The content of the message."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who sent the message."
        },
        "userEmail": {
          "type": "string",
          "description": "The email of the user who sent the message."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the message was created.",
          "format": "date-time"
        }
      },
      "required": ["text", "userId", "userEmail", "createdAt"]
    },
    "SupportTicket": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SupportTicket",
      "type": "object",
      "description": "A support ticket created by a user.",
      "properties": {
        "userId": {
          "type": "string"
        },
        "userEmail": {
          "type": "string"
        },
        "subject": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": ["open", "closed", "in-progress"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "replies": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "userId": {"type": "string"},
              "userEmail": {"type": "string"},
              "message": {"type": "string"},
              "createdAt": {"type": "string", "format": "date-time"},
              "isStaff": {"type": "boolean"}
            },
            "required": ["userId", "userEmail", "message", "createdAt", "isStaff"]
          }
        }
      },
      "required": ["userId", "userEmail", "subject", "message", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/coin_packages/{coinPackageId}",
        "definition": {
          "entityName": "CoinPackage",
          "schema": {
            "$ref": "#/backend/entities/CoinPackage"
          },
          "description": "Stores information about coin packages available for purchase.",
          "params": [
            {
              "name": "coinPackageId",
              "description": "Unique identifier for the coin package."
            }
          ]
        }
      },
      {
        "path": "/ranks/{rankId}",
        "definition": {
          "entityName": "Rank",
          "schema": {
            "$ref": "#/backend/entities/Rank"
          },
          "description": "Stores information about ranks available for purchase. Read access is public. Write access is restricted to authenticated admins.",
          "params": [
            {
              "name": "rankId",
              "description": "Unique identifier for the rank."
            }
          ]
        }
      },
      {
        "path": "/news_items/{newsItemId}",
        "definition": {
          "entityName": "NewsItem",
          "schema": {
            "$ref": "#/backend/entities/NewsItem"
          },
          "description": "Stores news items to be displayed on the Cloudverse store. Write access is restricted by an adminKey.",
          "params": [
            {
              "name": "newsItemId",
              "description": "Unique identifier for the news item."
            }
          ]
        }
      },
      {
        "path": "/feedback/{feedbackId}",
        "definition": {
          "entityName": "Feedback",
          "schema": {
            "$ref": "#/backend/entities/Feedback"
          },
          "description": "Stores user feedback submitted through the Cloudverse store.",
          "params": [
            {
              "name": "feedbackId",
              "description": "Unique identifier for the feedback entry."
            }
          ]
        }
      },
      {
        "path": "/staff/{staffId}",
        "definition": {
          "entityName": "StaffMember",
          "schema": {
            "$ref": "#/backend/entities/StaffMember"
          },
          "description": "Stores information about staff members. Read access is public. Write access is restricted by an adminKey. The security rule is: allow get, list: if true; allow create, update, delete: if request.resource.data.adminKey == 'cloudmcstaff';",
          "params": [
            {
              "name": "staffId",
              "description": "Unique identifier for the staff member."
            }
          ]
        }
      },
      {
        "path": "/chat_messages/{messageId}",
        "definition": {
            "entityName": "ChatMessage",
            "schema": { "$ref": "#/backend/entities/ChatMessage" },
            "description": "Stores live chat messages. Users must be authenticated to read or write."
        }
      },
      {
          "path": "/support_tickets/{ticketId}",
          "definition": {
              "entityName": "SupportTicket",
              "schema": { "$ref": "#/backend/entities/SupportTicket" },
              "description": "Stores support tickets. Users can only read/write their own tickets. Staff can read/write all tickets."
          }
      }
    ],
    "reasoning": "The Firestore structure is designed to support the Cloudverse store's data and access patterns, focusing on simplicity, security, and scalability. Authorization Independence is achieved by avoiding `get()` calls in security rules, ensuring atomic operations. All entities are placed in root-level collections to avoid hierarchical authorization. The structure supports QAPs by ensuring that listing operations can be securely implemented using simple rules based on document existence or equality checks rather than complex filtering.\n\nSpecifically:\n\n*   `coin_packages`: Stores coin package information.\n*   `ranks`: Stores rank information, write access controlled by admin status.\n*   `news_items`: Stores news items for display, write access controlled by `adminKey`.\n*   `feedback`: Stores user feedback. No authentication is required to submit feedback, facilitating ease of use.\n*   `staff`: Stores staff member information, read access is public, and write access controlled by `adminKey`.\n*   `chat_messages`: Stores live chat messages, accessible only to authenticated users.\n*   `support_tickets`: Manages support requests, with user-specific and staff-level permissions."
  }
}
